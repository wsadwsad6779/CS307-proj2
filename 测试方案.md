# CS307 Project 2 — Task 3(索引)& Task 4(事务)测试方案

> 运行环境：WSL2 + JDK 21 + Maven。所有命令在 `CS307-proj2/` 目录下执行。

---

## 0. 一键回归

```bash
mvn test
```
期望：`Tests run: 115, Failures: 0, Errors: 0`，`BUILD SUCCESS`。
覆盖 Task 4(TransactionManagerTest 10 例)、Task 3(BPlusTreeTest 3 例 + IndexSqlTest 1 例)及框架原有测试。

---

## 1. Task 3：索引（B+ 树）

### 1.1 自动化测试

| 测试类 | 覆盖点 | 运行命令 |
|---|---|---|
| `index.BPlusTreeTest` | 树本体：插入/查找/分裂/打印、删除(借位+合并)、删空复用、5000 步随机对拍 HashMap | `mvn test -Dtest=BPlusTreeTest` |
| `index.IndexSqlTest` | SQL 全链路：create/print/insert/delete/drop index 端到端 | `mvn test -Dtest=IndexSqlTest` |

**BPlusTreeTest 三个用例：**
- `insertSearchAndPrint`：插 1..10 → 打印 3 层树 → 查找全中；删 3,4,5 → 打印 → 验证已删的返回 null、其余仍在。
- `deleteAllThenReusable`：插 50 个 → 逐个删空(每步验证 null) → 再插 1 个能查到（树删空后仍可用）。
- `randomizedAgainstHashMap`：5000 步随机插入/删除，每步抽查 5 个 key，B+ 树结果必须与 `HashMap` 完全一致（**最强正确性保证**）。

### 1.2 手动演示（答辩用）— CLI 脚本

```bash
mvn exec:java -Dexec.mainClass=edu.sustech.cs307.DBEntry
```
依次输入：

```sql
create table t (id int);
insert into t (id) values (1);
insert into t (id) values (2);
insert into t (id) values (3);
insert into t (id) values (4);
insert into t (id) values (5);
insert into t (id) values (6);
insert into t (id) values (7);
insert into t (id) values (8);
create index ix on t (id);
print index ix;
```
**期望：基于已有 8 行建出 2 层树**
```
level0:I[3, 5, 7]
level1:L[1, 2] L[3, 4] L[5, 6] L[7, 8]
```

```sql
insert into t (id) values (9);
insert into t (id) values (10);
print index ix;
```
**期望：动态长成 3 层**
```
level0:I[7]
level1:I[3, 5] I[9]
level2:L[1, 2] L[3, 4] L[5, 6] L[7, 8] L[9, 10]
```

```sql
delete from t where id = 3;
delete from t where id = 4;
delete from t where id = 5;
print index ix;
```
**期望：借位/合并后无空节点**（路标随之更新）
```
level0:I[7]
level1:I[2, 5] I[9]
level2:L[1] L[2] L[6] L[7, 8] L[9, 10]
```

```sql
drop index ix;
print index ix;
```
**期望：`No such index: ix`**

### 1.3 要演示/检查的点（对照 PDF）

- [ ] 识别 `create index xx on t(col)`，识别 `drop index xx`
- [ ] meta_data.json 相应改变：建索引后 `CS307-DB/meta/meta_data.json` 里 t 表 `indexes` 出现 `{"ix":"BTREE"}`，drop 后消失
- [ ] 内存 B+ 树 + 能打印每个节点（`print index`）
- [ ] 多索引/多棵树：在另一列再 `create index ix2 on t(col2)`，两棵树独立
- [ ] 动态 insert/delete 时树同步更新（上面脚本已体现）
- [ ] 大数据集：插入 ≥1000 行后 `print index` 看多层结构（性能验证，见 1.4）

### 1.4 大数据 / 性能（PDF 要求）

CLI 里批量插入（或写脚本）≥1000 行后 `print index ix`，观察：树高 ~4 层、查询仍即时返回。
随机对拍测试（`randomizedAgainstHashMap`）已在代码层覆盖正确性与规模。

### 1.5 边界 / 异常用例

| 用例 | 操作 | 期望 |
|---|---|---|
| 对不存在的表建索引 | `create index x on nope(id)` | 抛 TABLE_DOES_NOT_EXIST |
| 对不存在的列建索引 | `create index x on t(nope)` | 抛 COLUMN_DOES_NOT_EXIST |
| drop 不存在的索引 | `drop index nope` | 日志 warn，不崩 |
| print 不存在的索引 | `print index nope` | 输出 `No such index: nope` |
| 重复 key | 同一 id 插两次 | 树里只一条，值更新为最新 RID |
| 建索引后普通查询 | `select * from t where id>5` | 正常返回（走全表扫描，不崩） |

---

## 2. Task 4：事务

### 2.1 自动化测试

```bash
mvn test -Dtest=TransactionManagerTest
```
期望 10 例全过。覆盖：rollback-to-savepoint、rollback 回到 begin 前、release 后不能再回滚、事务外 savepoint/rollback-to/release 报 TRANSACTION_REQUIRED、事务中再 begin 报 TRANSACTION_ALREADY_ACTIVE、rollback-to 保留目标点、同名 savepoint 栈遮蔽、事务外 commit/rollback 为 no-op。

### 2.2 手动演示 — CLI 脚本

```sql
create table u (id int);
begin;
insert into u (id) values (1);
savepoint a;
insert into u (id) values (2);
savepoint b;
insert into u (id) values (3);
rollback to savepoint b;   -- 撤回 id=3
commit;
select * from u;           -- 期望 1, 2
```

```sql
begin;
insert into u (id) values (99);
rollback;                  -- 整体撤回
select * from u;           -- 99 不存在
```

---

## 3. 答辩 Q&A 检查单

**B+ 树**
- 为什么数据只在叶子？内部节点只做路标导航，叶子串成链表便于范围扫。
- 插入满了怎么办？分裂；叶子复制中间 key 上推、内部移动中间 key 上推；根爆了造新根长高。
- 为什么树总是平衡？只有根分裂才长高，所有叶子同时下沉一层，始终同层。
- 删除空了怎么办？下溢时优先向兄弟借（兄弟 key>MIN）、否则与兄弟合并；合并使父少一个路标，可能继续上溯；根删空则变矮。
- 借位与合并的区别？借位只挪一个 key 并改父路标；合并把两节点合一并删父路标。
- 内部合并要把父路标拉下来、叶子合并直接丢父路标——为什么？叶子路标是副本，内部路标是真实分隔键。

**索引集成**
- 树存哪？内存 IndexManager（`表→列→树`），不落盘（PDF 要 in-memory）。JSON 只记“有此索引”。
- insert/delete 如何联动？InsertOperator 拿 InsertRecord 返回的 RID 插树；DeleteOperator 扫描时读列值删树。
- 多棵树？IndexManager 的嵌套 Map 天然支持多表多列。

**事务（shadow paging）**
- 实现方式？每个 savepoint/begin 把整个 DB 目录快照到临时目录；回滚=拷回快照 + 同步内存（discardAllPages + metaManager.reload + 还原 filePages）。
- savepoint 栈语义？ArrayList 当栈，从栈顶往下找实现同名遮蔽；rollback-to 保留目标点、release 连目标点一起删。
